<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8"/>
<title>LIFELINEMETA - SUPER ADMIN PANEL</title>
<meta name="viewport" content="width=device-width, initial-scale=1"/>

<style>

/* ================= RESET ================= */

*{
margin:0;
padding:0;
box-sizing:border-box;
font-family:Segoe UI,sans-serif;
}

/* ================= BODY ================= */

body{
background:#f1f5f9;
color:#1f2937;
}

/* ================= SIDEBAR ================= */

.sidebar{
width:270px;
background:#1e40af;
color:white;
position:fixed;
top:0;
left:0;
bottom:0;
padding:20px;
overflow:auto;
}

.logo{
font-size:24px;
font-weight:800;
text-align:center;
margin-bottom:30px;
padding-bottom:15px;
border-bottom:1px solid rgba(255,255,255,0.2);
}

.menu-item{
padding:15px;
border-radius:10px;
cursor:pointer;
margin-bottom:10px;
transition:0.3s;
font-weight:600;
}

.menu-item:hover{
background:#2563eb;
transform:translateX(5px);
}

/* ================= MAIN ================= */

.main{
margin-left:270px;
padding:25px;
}

/* ================= HEADER ================= */

.header{
background:white;
padding:20px;
border-radius:12px;
box-shadow:0 5px 20px rgba(0,0,0,0.08);
margin-bottom:20px;
display:flex;
justify-content:space-between;
align-items:center;
}

/* ================= CARD ================= */

.card{
background:white;
padding:20px;
border-radius:12px;
box-shadow:0 5px 20px rgba(0,0,0,0.08);
margin-bottom:20px;
}

/* ================= GRID ================= */

.grid{
display:grid;
grid-template-columns:repeat(auto-fit,minmax(220px,1fr));
gap:20px;
margin-bottom:20px;
}

/* ================= TABLE ================= */

table{
width:100%;
border-collapse:collapse;
}

th,td{
padding:12px;
border-bottom:1px solid #eee;
text-align:left;
font-size:14px;
}

th{
background:#eff6ff;
color:#1e40af;
}

/* ================= BUTTON ================= */

button{
padding:8px 14px;
border:none;
border-radius:6px;
cursor:pointer;
font-weight:600;
margin:2px;
}

.green{
background:#10b981;
color:white;
}

.red{
background:#ef4444;
color:white;
}

.blue{
background:#1e40af;
color:white;
}

.orange{
background:#f59e0b;
color:white;
}

/* ================= INPUT ================= */

input,textarea,select{
padding:10px;
width:100%;
margin-top:5px;
border:1px solid #ddd;
border-radius:8px;
}

textarea{
min-height:120px;
resize:none;
}

/* ================= PAGE ================= */

.page{
display:none;
}

.active{
display:block;
}

/* ================= BADGE ================= */

.badge-green{
background:#dcfce7;
color:#166534;
padding:5px 10px;
border-radius:20px;
font-size:12px;
font-weight:700;
}

.badge-red{
background:#fee2e2;
color:#991b1b;
padding:5px 10px;
border-radius:20px;
font-size:12px;
font-weight:700;
}

/* ================= NOTICE ================= */

.notice-box{
background:#eff6ff;
padding:15px;
border-radius:10px;
margin-top:10px;
}

/* ================= SEARCH ================= */

.search-box{
margin:15px 0;
}

</style>
</head>

<body>

<script>
if(localStorage.getItem("adminLogged") !== "true"){
window.location.href="admin-login.html";
}
</script>

<!-- ================= SIDEBAR ================= -->

<div class="sidebar">

<div class="logo">
⚙️ ADMIN PANEL
</div>

<div class="menu-item" onclick="showPage('dashboard')">📊 Dashboard</div>

<div class="menu-item" onclick="showPage('users')">👥 Users</div>

<div class="menu-item" onclick="showPage('investments')">📈 Investments</div>

<div class="menu-item" onclick="showPage('withdraw')">💸 Withdrawals</div>

<div class="menu-item" onclick="showPage('notice')">📢 Notice Board</div>

<div class="menu-item" onclick="showPage('settings')">⚙️ Global Settings</div>

<div class="menu-item" onclick="showPage('menucontrol')">🧩 Menu Control</div>

<div class="menu-item" onclick="showPage('addbalance')">💰 Add Balance</div>

<div class="menu-item" onclick="showPage('createuser')">➕ Create User</div>

<div class="menu-item" onclick="logout()">🚪 Logout</div>

</div>

<!-- ================= MAIN ================= -->

<div class="main">

<div class="header">
<h2>SUPER ADMIN DASHBOARD</h2>
<div>Welcome Admin 👑</div>
</div>

<!-- ================= DASHBOARD ================= -->

<div id="dashboard" class="page active">

<div class="grid">

<div class="card">
<h3>Total Users</h3>
<h1 id="totalUsers">0</h1>
</div>

<div class="card">
<h3>Total Investment</h3>
<h1 id="totalInvest">$0</h1>
</div>

<div class="card">
<h3>Pending Withdraw</h3>
<h1 id="pendingWithdraw">0</h1>
</div>

<div class="card">
<h3>Total Withdraw</h3>
<h1 id="totalWithdraw">$0</h1>
</div>

</div>

<div class="card">

<h2>📢 Live Notice</h2>

<div class="notice-box" id="liveNotice">
welcome. Have a nice day
</div>

</div>

</div>

<!-- ================= USERS ================= -->

<div id="users" class="page">

<div class="card">

<h2>All Users</h2>

<div class="search-box">

<input
type="text"
id="searchUser"
placeholder="Search user..."
onkeyup="searchUsers()">

</div>

<table>

<thead>

<tr>
<th>Name</th>
<th>Email</th>
<th>Balance</th>
<th>Rank</th>
<th>Status</th>
<th>Income</th>
<th>Action</th>
</tr>

</thead>

<tbody id="userTable"></tbody>

</table>

</div>

</div>

<!-- ================= INVESTMENTS ================= -->

<div id="investments" class="page">

<div class="card">

<h2>All Investments</h2>

<table>

<thead>

<tr>
<th>User</th>
<th>Amount</th>
<th>Date</th>
<th>Status</th>
</tr>

</thead>

<tbody id="investTable"></tbody>

</table>

</div>

</div>

<!-- ================= WITHDRAW ================= -->

<div id="withdraw" class="page">

<div class="card">

<h2>Withdraw Requests</h2>

<table>

<thead>

<tr>
<th>User</th>
<th>Amount</th>
<th>Status</th>
<th>Action</th>
</tr>

</thead>

<tbody id="withdrawTable"></tbody>

</table>

</div>

</div>

<!-- ================= NOTICE ================= -->

<div id="notice" class="page">

<div class="card">

<h2>Notice Board</h2>

<label>Write Notice</label>

<textarea id="noticeText"></textarea>

<br><br>

<button class="blue" onclick="saveNotice()">
Save Notice
</button>

</div>

</div>
  <button class="green" onclick="distributeROI()">
💰 Monthly ROI Distribution
</button>
<!-- ================= SETTINGS ================= -->

<div id="settings" class="page">

<div class="card">

<h2>Global MLM Settings</h2>

<label>ROI (%)</label>
<input id="roi">

<br><br>

<label>Referral Bonus (%)</label>
<input id="ref">

<br><br>

<label>Registration Fee ($)</label>
<input id="fee">

<br><br>

<label>Minimum Withdraw ($)</label>
<input id="minWithdraw">

<br><br>

<label>Maximum Withdraw ($)</label>
<input id="maxWithdraw">

<br><br>

<label>Monthly ROI (%)</label>
<input id="monthlyROI">

<br><br>

<button class="blue" onclick="saveSettings()">
Save Settings
</button>

</div>

</div>

<!-- ================= MENU CONTROL ================= -->

<div id="menucontrol" class="page">

<div class="card">

<h2>User Dashboard Menu Control</h2>

<label>Deposit Button</label>
<select id="depositBtn">
<option value="true">ON</option>
<option value="false">OFF</option>
</select>

<br><br>

<label>Withdraw Button</label>
<select id="withdrawBtn">
<option value="true">ON</option>
<option value="false">OFF</option>
</select>

<br><br>

<label>Team Button</label>
<select id="teamBtn">
<option value="true">ON</option>
<option value="false">OFF</option>
</select>

<br><br>

<label>Salary Button</label>
<select id="salaryBtn">
<option value="true">ON</option>
<option value="false">OFF</option>
</select>

<br><br>

<button class="blue" onclick="saveMenuControl()">
Save Menu Control
</button>

</div>

</div>

<!-- ================= ADD BALANCE ================= -->

<div id="addbalance" class="page">

<div class="card">

<h2>Add User Balance</h2>

<label>User Document ID</label>
<input id="balanceUserId">

<br><br>

<label>Amount</label>
<input id="balanceAmount">

<br><br>

<button class="green" onclick="addBalance()">
Add Balance
</button>

</div>

</div>

<!-- ================= CREATE USER ================= -->

<div id="createuser" class="page">

<div class="card">

<h2>Create New User</h2>

<label>Name</label>
<input id="newName">

<br><br>

<label>Email</label>
<input id="newEmail">

<br><br>

<label>Balance</label>
<input id="newBalance">

<br><br>

<label>Rank</label>
<input id="newRank" placeholder="Member">

<br><br>

<button class="blue" onclick="createUser()">
Create User
</button>

</div>

</div>

</div>

<!-- ================= FIREBASE ================= -->

<script type="module">

import { initializeApp }
from "https://www.gstatic.com/firebasejs/9.6.10/firebase-app.js";

import {
getFirestore,
collection,
getDocs,
doc,
updateDoc,
setDoc,
getDoc,
deleteDoc,
addDoc
}
from "https://www.gstatic.com/firebasejs/9.6.10/firebase-firestore.js";

/* ================= FIREBASE CONFIG ================= */

const firebaseConfig = {

  apiKey: "AIzaSyA3G2bMOJ_HSDYXJPuM70tVjfbkHlMftr4",
  authDomain: "livelinemeta.firebaseapp.com",
  projectId: "livelinemeta",
  storageBucket: "livelinemeta.appspot.com",   // ✅ ঠিক করা হলো
  messagingSenderId: "206900805598",
  appId: "1:206900805598:web:10e6b39100af201b4e674f"

};

const app = initializeApp(firebaseConfig);

const db = getFirestore(app);

/* ================= PAGE SWITCH ================= */

window.showPage = (id)=>{

document.querySelectorAll(".page").forEach((page)=>{
page.classList.remove("active");
});

document.getElementById(id).classList.add("active");

};

/* ================= SEARCH USERS ================= */

window.searchUsers = ()=>{

let input =
document.getElementById("searchUser")
.value.toLowerCase();

let rows =
document.querySelectorAll("#userTable tr");

rows.forEach((row)=>{

let text = row.innerText.toLowerCase();

row.style.display =
text.includes(input)
? ""
: "none";

});

};

/* ================= LOAD USERS ================= */

async function loadUsers(){

const snap = await getDocs(collection(db,"users"));

let html = "";
let count = 0;

snap.forEach((d)=>{

count++;

const data = d.data();

html += `

<tr>

<td>${data.name || "-"}</td>

<td>${data.email || "-"}</td>

<td>$${data.balance || 0}</td>

<td>${data.rank || "Member"}</td>

<td>

${data.blocked ?

'<span class="badge-red">Blocked</span>'

:

'<span class="badge-green">Active</span>'

}

</td>

<td>

${data.incomeEnabled === false ?

'<span class="badge-red">OFF</span>'

:

'<span class="badge-green">ON</span>'

}

</td>

<td>

<button class="blue"
onclick="toggleIncome('${d.id}',${data.incomeEnabled})">

${data.incomeEnabled === false
? 'Income OFF'
: 'Income ON'}

</button>

<button class="orange"
onclick="blockUser('${d.id}',${data.blocked})">

${data.blocked
? 'Unblock'
: 'Block'}

</button>

<button class="blue"
onclick="loginAsUser('${d.id}')">
Login
</button>

<button class="red"
onclick="deleteUser('${d.id}')">
Delete
</button>

</td>

</tr>

`;

});

document.getElementById("userTable").innerHTML = html;

document.getElementById("totalUsers").innerText = count;

}

/* ================= BLOCK USER ================= */

window.blockUser = async(id,status)=>{

await updateDoc(doc(db,"users",id),{
blocked: !status
});

alert("User Updated");

loadUsers();

};

/* ================= INCOME CONTROL ================= */

window.toggleIncome = async(id,status)=>{

await updateDoc(doc(db,"users",id),{
incomeEnabled: !status
});

alert("Income Updated");

loadUsers();

};

/* ================= DELETE USER ================= */

window.deleteUser = async(id)=>{

let ok = confirm("Delete User?");

if(!ok) return;

await deleteDoc(doc(db,"users",id));

alert("User Deleted");

loadUsers();

};

/* ================= QUICK BALANCE ================= */

window.quickAddBalance = async(id)=>{

let amount = prompt("Enter Amount");

if(!amount) return;

const userRef = doc(db,"users",id);

const snap = await getDoc(userRef);

if(snap.exists()){

let current =
Number(snap.data().balance || 0);

await updateDoc(userRef,{
balance:
current + Number(amount)
});

alert("Balance Added");

loadUsers();

}

};

/* ================= ADD BALANCE PAGE ================= */

window.addBalance = async()=>{

const userId =
document.getElementById("balanceUserId").value;

const amount =
document.getElementById("balanceAmount").value;

if(!userId || !amount){

alert("Fill All Fields");

return;

}

const userRef = doc(db,"users",userId);

const snap = await getDoc(userRef);

if(!snap.exists()){

alert("User Not Found");

return;

}

let current =
Number(snap.data().balance || 0);

await updateDoc(userRef,{
balance:
current + Number(amount)
});

alert("Balance Added");

loadUsers();

};

/* ================= CREATE USER ================= */

window.createUser = async()=>{

const name =
document.getElementById("newName").value;

const email =
document.getElementById("newEmail").value;

const balance =
document.getElementById("newBalance").value;

const rank =
document.getElementById("newRank").value;

if(!name || !email){

alert("Fill All Fields");

return;

}

await addDoc(collection(db,"users"),{

name,
email,

balance:Number(balance || 0),

rank:rank || "Member",

blocked:false,

incomeEnabled:true,

createdAt:new Date().toISOString()

});

alert("User Created");

loadUsers();

};
/* ================= EDIT USER ================= */

window.editUser = async(id)=>{

const userRef = doc(db,"users",id);

const snap = await getDoc(userRef);

if(snap.exists()){

const data = snap.data();

let newName =
prompt("Edit Name", data.name);

if(newName === null) return;

let newBalance =
prompt("Edit Balance", data.balance);

if(newBalance === null) return;

let newRank =
prompt("Edit Rank", data.rank || "Member");

if(newRank === null) return;

await updateDoc(userRef,{

name:newName,

balance:Number(newBalance),

rank:newRank

});

alert("User Updated");

loadUsers();

}

};

/* ================= LOAD INVESTMENTS ================= */

async function loadInvestments(){

const snap =
await getDocs(collection(db,"investments"));

let html = "";
let total = 0;

snap.forEach((d)=>{

const data = d.data();

total += Number(data.amount || 0);

html += `

<tr>

<td>${data.user || "-"}</td>

<td>$${data.amount || 0}</td>

<td>${data.date || "-"}</td>

<td>${data.status || "active"}</td>

</tr>

`;

});

document.getElementById("investTable").innerHTML = html;

document.getElementById("totalInvest").innerText =
"$" + total;

}

/* ================= LOAD WITHDRAW ================= */

async function loadWithdraw(){

const snap =
await getDocs(collection(db,"withdrawals"));

let html = "";

let pending = 0;

let total = 0;

snap.forEach((d)=>{

const data = d.data();

total += Number(data.amount || 0);

if(data.status !== "approved"){
pending++;
}

html += `

<tr>

<td>${data.user || "-"}</td>

<td>$${data.amount || 0}</td>

<td>${data.status || "pending"}</td>

<td>

<button class="green"
onclick="approveWithdraw('${d.id}')">
Approve
</button>

<button class="red"
onclick="rejectWithdraw('${d.id}')">
Reject
</button>

</td>

</tr>

`;

});

document.getElementById("withdrawTable").innerHTML = html;

document.getElementById("pendingWithdraw").innerText =
pending;

document.getElementById("totalWithdraw").innerText =
"$" + total;

}

/* ================= APPROVE ================= */

window.approveWithdraw = async(id)=>{

await updateDoc(doc(db,"withdrawals",id),{
status:"approved"
});

alert("Approved");

loadWithdraw();

};

/* ================= REJECT ================= */

window.rejectWithdraw = async(id)=>{

await updateDoc(doc(db,"withdrawals",id),{
status:"rejected"
});

alert("Rejected");

loadWithdraw();

};

/* ================= SETTINGS ================= */

window.saveSettings = async()=>{

const roi =
document.getElementById("roi").value;

const ref =
document.getElementById("ref").value;

const fee =
document.getElementById("fee").value;

const minWithdraw =
document.getElementById("minWithdraw").value;

const maxWithdraw =
document.getElementById("maxWithdraw").value;

const monthlyROI =
document.getElementById("monthlyROI").value;

await setDoc(doc(db,"settings","global"),{

roi,

referralBonus:ref,

registrationFee:fee,

minWithdraw,

maxWithdraw,

monthlyROI

});

alert("Settings Saved");

};

/* ================= LOAD SETTINGS ================= */

async function loadSettings(){

const snap =
await getDoc(doc(db,"settings","global"));

if(snap.exists()){

const data = snap.data();

document.getElementById("roi").value =
data.roi || "";

document.getElementById("ref").value =
data.referralBonus || "";

document.getElementById("fee").value =
data.registrationFee || "";

document.getElementById("minWithdraw").value =
data.minWithdraw || "";

document.getElementById("maxWithdraw").value =
data.maxWithdraw || "";

document.getElementById("monthlyROI").value =
data.monthlyROI || "";

}

}

/* ================= NOTICE ================= */

window.saveNotice = async()=>{

const notice =
document.getElementById("noticeText").value;

await setDoc(doc(db,"notice","latest"),{
text:notice
});

alert("Notice Saved");

loadNotice();

};

async function loadNotice(){

const snap =
await getDoc(doc(db,"notice","latest"));

if(snap.exists()){

const data = snap.data();

document.getElementById("liveNotice").innerText =
data.text;

document.getElementById("noticeText").value =
data.text;

}

}

/* ================= MENU CONTROL ================= */

window.saveMenuControl = async()=>{

const depositBtn =
document.getElementById("depositBtn").value;

const withdrawBtn =
document.getElementById("withdrawBtn").value;

const teamBtn =
document.getElementById("teamBtn").value;

const salaryBtn =
document.getElementById("salaryBtn").value;

await setDoc(doc(db,"menuControl","main"),{

deposit:
depositBtn === "true",

withdraw:
withdrawBtn === "true",

team:
teamBtn === "true",

salary:
salaryBtn === "true"

});

alert("Menu Updated");

};
/* ================= LOAD MENU CONTROL ================= */

async function loadMenuControl(){

const snap =
await getDoc(doc(db,"menuControl","main"));

if(snap.exists()){

const data = snap.data();

document.getElementById("depositBtn").value =
String(data.deposit);

document.getElementById("withdrawBtn").value =
String(data.withdraw);

document.getElementById("teamBtn").value =
String(data.team);

document.getElementById("salaryBtn").value =
String(data.salary);

}

}

/* ================= LOGOUT ================= */

window.logout = ()=>{

localStorage.removeItem("adminLogged");

window.location.href = "admin-login.html";

};

/* ================= INIT ================= */

loadUsers();

loadInvestments();

loadWithdraw();

loadSettings();

loadNotice();
loadMenuControl();

</script>

</body>
</html>




dashboard
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>LIFELINEMETA – Register</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    *{margin:0;padding:0;box-sizing:border-box;}
    body{
      min-height:100vh;
      background:#ffffff;
      font-family:system-ui, sans-serif;
      display:flex;
      flex-direction:column;
      align-items:center;
    }
    .header{
      width:100%; max-width:900px; padding:12px 20px;
      display:flex; align-items:center; gap:12px;
      background:white;
    }
    .logo{
      width:65px; height:65px; border-radius:50%; 
      object-fit:cover; border:2px solid #1e3a8a;
    }
    .title{font-size:24px; font-weight:800; color:#000000;} /* Smaller title */

    .container{width:100%; max-width:480px; padding:20px 15px; margin-top:20px;}
    .card{
      background:white; border-radius:16px; padding:30px 25px;
      box-shadow:0 5px 20px rgba(0,0,0,0.08); border:2px solid #1e3a8a;
    }
    h1{
      text-align:center; 
      color:#000000; 
      margin-bottom:25px; 
      font-size:23px;
    }
    .form-group{margin-bottom:16px; position:relative;}
    label{font-size:14px; font-weight:600; color:#000000; margin-bottom:6px; display:block;}
    input, select{
      width:100%; padding:14px 14px 14px 45px; border:1px solid #1e3a8a;
      border-radius:8px; font-size:15px; background:#f9fafb;
    }
    .icon{
      position:absolute; left:15px; top:38px; font-size:18px; 
      color:#000000; /* All symbols black */
    }
    .eye{
      position:absolute; right:15px; top:38px; cursor:pointer;
      font-weight:bold; color:#1e3a8a;
    }
    .strength, .match{font-size:13px; margin-top:5px; font-weight:600;}
    button{
      width:100%; padding:15px; background:#1e3a8a; color:white;
      border:none; border-radius:8px; font-size:16px; font-weight:700;
      margin-top:10px; cursor:pointer;
    }
    button:hover{background:#2563eb;}
    #message{text-align:center; margin:15px 0; font-weight:600; min-height:50px;}
  </style>
</head>
<body>

<div class="header">
  <img src="assets/logo/livelinemeta.png" alt="Logo" class="logo">
  <h1 class="title">LIVELINEMETA</h1>
</div>

<div class="container">
  <div class="card">
    <h1>CREATE ACCOUNT <span style="font-size:24px;">📝</span></h1> <!-- Black symbol added -->
    
    <form id="registerForm">
      <div class="form-group">
        <label>Full Name</label>
        <span class="icon">👤</span>
        <input type="text" id="fullName" placeholder="Full Name" required>
      </div>

      <div class="form-group">
        <label>Username</label>
        <span class="icon">@</span>
        <input type="text" id="username" placeholder="Username" required>
      </div>

      <div class="form-group">
        <label>Email</label>
        <span class="icon">✉️</span>
        <input type="email" id="email" placeholder="Email Address" required>
      </div>

      <div class="form-group">
        <label>Mobile Number</label>
        <span class="icon">📱</span>
        <input type="tel" id="mobile" placeholder="Mobile Number" required>
      </div>

      <div class="form-group">
        <label>Country</label>
        <span class="icon">🌍</span>
        <select id="country" required>
          <option value="Bangladesh">Bangladesh</option>
          <option value="India">India</option>
          <option value="USA">USA</option>
          <option value="UK">UK</option>
        </select>
      </div>

      <div class="form-group">
        <label>Password</label>
        <span class="icon">🔒</span>
        <input type="password" id="password" placeholder="Password" required>
        <span class="eye" data-target="password">SHOW</span>
        <div id="strength" class="strength"></div>
      </div>

      <div class="form-group">
        <label>Confirm Password</label>
        <span class="icon">🔒</span>
        <input type="password" id="password2" placeholder="Confirm Password" required>
        <span class="eye" data-target="password2">SHOW</span>
        <div id="match" class="match"></div>
      </div>

      <div class="form-group">
        <label>Referral Code (Optional)</label>
        <span class="icon">🔗</span>
        <input type="text" id="refCode" placeholder="Referral Code">
      </div>

      <label><input type="checkbox" required> I agree to Terms & Conditions</label>
      
      <button type="submit">REGISTER NOW</button>
    </form>
    
    <div id="message"></div>
  </div>
</div>

<!-- Firebase -->
<script src="https://www.gstatic.com/firebasejs/9.6.10/firebase-app-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/9.6.10/firebase-auth-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/9.6.10/firebase-firestore-compat.js"></script>

<script>
// Firebase Config + All functions (same as before)
const firebaseConfig = {
  apiKey: "AIzaSyA3G2bMOJ_HSDYXJPuM70tVjfbkHlMftr4",
  authDomain: "livelinemeta.firebaseapp.com",
  projectId: "livelinemeta",
  storageBucket: "livelinemeta.appspot.com",
  messagingSenderId: "206900805598",
  appId: "1:206900805598:web:10e6b39100af201b4e674f"
};

firebase.initializeApp(firebaseConfig);
const auth = firebase.auth();
const db = firebase.firestore();

// Password Show/Hide
document.querySelectorAll('.eye').forEach(icon => {
  icon.addEventListener('click', () => {
    const target = document.getElementById(icon.dataset.target);
    if (target.type === 'password') {
      target.type = 'text';
      icon.textContent = 'HIDE';
    } else {
      target.type = 'password';
      icon.textContent = 'SHOW';
    }
  });
});

// Password Strength & Match (same)
const password = document.getElementById('password');
const password2 = document.getElementById('password2');
const strengthBox = document.getElementById('strength');
const matchBox = document.getElementById('match');

password.addEventListener('input', () => {
  const val = password.value;
  if (val.length === 0) strengthBox.textContent = '';
  else if (val.length >= 8 && /[A-Z]/.test(val) && /\d/.test(val) && /[^A-Za-z0-9]/.test(val)) {
    strengthBox.innerHTML = '✅ <span style="color:green">Strong</span>';
  } else if (val.length >= 6) {
    strengthBox.innerHTML = '⚠️ <span style="color:orange">Medium</span>';
  } else {
    strengthBox.innerHTML = '❌ <span style="color:red">Weak</span>';
  }
  checkMatch();
});

password2.addEventListener('input', checkMatch);

function checkMatch() {
  if (password2.value === "") matchBox.textContent = "";
  else if (password.value === password2.value) {
    matchBox.innerHTML = '✅ <span style="color:green">Passwords Match</span>';
  } else {
    matchBox.innerHTML = '❌ <span style="color:red">Passwords Do Not Match</span>';
  }
}

// Form Submit (same working code)
document.getElementById("registerForm").addEventListener("submit", async (e) => {
  e.preventDefault();
  const message = document.getElementById("message");
  message.style.color = "blue";
  message.textContent = "Creating account... Please wait";

  const fullName = document.getElementById("fullName").value.trim();
  const username = document.getElementById("username").value.trim();
  const email = document.getElementById("email").value.trim();
  const passwordVal = password.value;
  const password2Val = password2.value;

  if (passwordVal !== password2Val) {
    message.style.color = "red";
    message.textContent = "❌ Passwords do not match!";
    return;
  }
  if (passwordVal.length < 6) {
    message.style.color = "red";
    message.textContent = "❌ Password must be at least 6 characters!";
    return;
  }

  try {
    const userCredential = await auth.createUserWithEmailAndPassword(email, passwordVal);
    const uid = userCredential.user.uid;

    await db.collection("users").doc(uid).set({
      fullName, username, email,
      country: document.getElementById("country").value,
      mobile: document.getElementById("mobile").value,
      refCode: document.getElementById("refCode").value || "",
      createdAt: new Date(),
      balance: 0
    });

    message.style.color = "green";
    message.innerHTML = `✅ Registration Successful!<br>Your ID: ${uid}<br>Redirecting...`;

    setTimeout(() => window.location.href = `login.html?uid=${uid}`, 2000);
  } catch (error) {
    console.error(error);
    message.style.color = "red";
    message.textContent = "❌ " + error.message;
  }
});
</script>
</body>
</html>    now 20 june
